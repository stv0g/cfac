package sensor_community

const (
	UrlAPI      = "https://maps.sensor.community/data/v1"
	UrlAPINoise = UrlAPI + "/data.noise.json"
	UrlAPITemp  = UrlAPI + "/data.temp.json"
	UrlAPIDust  = UrlAPI + "/data.dust.json"
)
